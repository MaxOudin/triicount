<h2>New Expense</h2>

<%= simple_form_for [@tricount, @expense], url: user_tricounts_path(@tricount.user, @tricount) do |f| %>
  <%= f.input :title, label: 'Expense title' %>
  <%= f.input :description, label: 'Expense description' %>

  <!-- Champ pour les participants avec le modèle participant -->
  <%= f.button :submit %>
<% end %>
